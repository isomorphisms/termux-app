# Build project

The Android/Gradle project lives here so the repository root can stay focused on source.

Build from this directory, for example:

```sh
cd _
./gradlew test
./gradlew assembleDebug
```

The repository root contains short symbolic links into the main source packages and resources.
